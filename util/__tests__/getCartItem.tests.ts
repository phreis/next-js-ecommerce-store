// Unit: Test function that combines the product data from the database with the product quantity data from your cookie

/* import { expect, test } from '@jest/globals';
import { getCartDataServerAction } from '../../app/serverActions';

async function init() {
  // const foo = await clearCartItemServerAction();
  const bar = await getCartDataServerAction();
  console.log(bar);
}

init();

test('cart is empty', () => {
  // expect(add(1, 1)).toBe(2);
  // expect(add(100, 100)).toBe(200);
}); */
